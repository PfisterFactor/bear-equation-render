{
  "name": "equation-render",
  "version": "1.0.0",
  "description": "Renders math equations from text and puts them in the bear note taking app",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Eric Pfister",
  "license": "MIT",
  "devDependencies": {
    "electron": "^9.4.0"
  },
  "dependencies": {
    "mathjax-node": "^2.1.1"
  }
}
